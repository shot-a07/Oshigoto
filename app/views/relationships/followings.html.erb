<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h3>@<%= current_user.name %>さんがフォローしている人</h3>
      <% if @users.exists? %>
        <%= render 'users/index', users: @users %>
      <% else %>
      <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>