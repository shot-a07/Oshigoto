<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1>Follower Users</h1>
      <% if @users.exists? %>
        <%= render 'users/index', users: @users %>
      <% else %>
      <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>