<div class="container">
    <div class="row">
      <div class="col-sm-8">
          <h2>Follower Users</h2>
          <% if @users.exists? %>
            <%= render 'users/index', users: @users %>
          <% else %>
          <p>ユーザーはいません</p>
          <% end %>
       </div>
    </div>
</div>